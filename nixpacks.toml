# nixpacks.toml

[variables]
NIXPACKS_NODE_VERSION = "20"

[phases.install]
# Garante devDependencies durante o build
cmd = "npm ci --include=dev"

[phases.build]
# Gera Prisma e compila o Next
cmd = "npx prisma generate && npm run build"

[start]
cmd = "npm start"
